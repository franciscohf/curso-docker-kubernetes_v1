apiVersion: v1
kind: Service
metadata:
  name: products-api-service
  labels:
    app: products-api
spec:
  type: NodePort
  selector:
    app: products-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
